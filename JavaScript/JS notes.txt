JavaScript:
------------
->JavaScript is the scripoting language as well as programming language

why we need JS:
-----------------
->to build the web application
->to add functionality to the web application
->to manipulate HTML and CSS
->to achieve the DOM (Document object model) functionalities
->to fetch data from server(API)  and also to send  data to server JS is required

difference b/w java and JavaScript:
-------------------------------------
           java                                 |                       JavaScript
--------------------------------------------    |   --------------------------------------------------
-> java object oriented programming             |    -> JS is the scripting language as well as object
   language                                     |       based programming language
-> in both compiler and interpreter present     |    -> it have only interpreter      
-> java will support multithreading             |    -> JS will support single call stack
-> java is a immperative nature                 |    -> JS is the dynamically typed(in JS no need define 
                                                |       the data types or data type to th variable)
-> java is strictly typed                       |    -> Js is weekly typed
-> java will support the interface              |    -> JS does not  support the interface

Tokens:
--------
->tokens are the smallest unity/ elements in the JS programming

Classifications:
------------------
1. Identifiers :
 ->Identifiers are the names given to JS code 
 ex:variable names, fn names, class names, funcyion names
 ->In theb place of identifiers we cant't use keywords
 rules:
 ------
 
2. literals :
->literals are the values given to the JS code
TYPES:
    1.String literals
    2.Numeric literals

3. keywords :
->keywords are the predefine words which having it's meaning
->in JS total 66 keywords present
Ex:
    >int, char, function, float, boolean, class, const, var, let, if, else, for, while, do, continue, switch, break, default, double, exports, finally, try, catch, super, this, throw, package, public, void, null, import, get, set, abstract, assert, byte,  constant, enum, extence, delete, implement etc.. 

4. separators :
->seperators are used to seperate the given code
Ex:{}, [],(), :, ;, ', ",<>, ``

5. operators :
->operators are used to preform operation on operands
TYPES:
    1.Assignment operators (=, +=, -=, *=, /=, %=)
    2.relational operators (>, >=, <, <=)
    3.Arthematic operators (+, -,*, %, **)
    4.bitwise operators (&, |, ^)
    5.logic operators (&&, ||, !)
    6.unary operators (++, --)
    7.ternary operators (conditional? expr1:expr2)(<<, >>)
    8.shift operators (<<, >>)
    9.conditional operators (==, ===, !=)

6. comments :
->comments are used to comments the given codeTYPES:
    1.single line comments(//)
    2.multi line comments(/* */)

DataTypes :
-----------
->DataTypes represents the what type of data we storing on variable
TYPES:
    1.primitive DataTypes (immutable) :
        1.number
        2.String
        3.boolean
        4.null
        5.undefined
        6.symbol
        7.bigInt

    2.Non-primitive DataTypes(mutable, object de-referencing) :
        1.Arrays
        2.objects
        3.functions
        4.Class type

variable:
----------
->variables named memory location used to store data or values and it can change N number of times during execution

        str="JavaScript"
        str=10
    1.variable Declaration
    2.variable initialization
    3.variable utilization
    4.variable Re-initialization
    5.variable Re-Declaration

in JS we define variables in 3days:
    -------------------------------------
    1. let  
        -> let variables acts as like a local scop/ block scop
        -> variable Declaration and initialization is same/ different line is posible
        ->re-initialization is posible but re-Declaration is not posible
        ->we can't hoist the let variables

    2. var
        ->var variables acts as like a global scop( because var present in windows object)
        -> variable Declaration and initialization is same/ different line is posible 
        ->re-initialization is posible and re-Declaration is not posible
        ->we can hoist the var variables
    3. const
        ->const variables acts as like a final  ( because once initialization values we can't modify data/values)
        -> variable Declaration and initialization is same/but different line is not posible 
        ->re-initialization and re-Declaration is not posible
        ->we can't hoist the const variables


    hoisting:
    ----------
    ->utilizing the variables before Declaration
    ->hoisting is posible
        1.variable Declaration
        2.variable initilization
        3.variable utilization
        4.variable Re-initilization
        5.variable Re-Declaration

    Strings:
    ---------
    ->strings are group of characters
    ->we can declare string 3 ways
    1. ""
    2. ''
    3. ``  ->template string  & string interplo

    stringg inbuilt functions:
    ----------------------------
    ->Arrays are linear data structurs used to homogenous types of data
    ->Array size is not fixed

    Ways to declare Arrays:
    -------------------------
    1. literal ways
    2.using new keyword (Array Object)

    Functions/methods:
    -------------------- 
     ->functions is the block of statements which will get execute when ever we called or invoke 
     ->using functions we can increase code readability and code reusability

     basically we have 2 types of functions:
         1. user define functions
               ->any functions that declared by user or programmer as per their requirements
         2. predefine functions
               ->predefine  functions means inbuilt functions

JS  function types:
--------------------
1. anymous function
2. normal function
3. functional expression
4. immediate invoking function/ self calling function
5. allow function
6. nested function
7. higher order function
8. callback function
9. generator function